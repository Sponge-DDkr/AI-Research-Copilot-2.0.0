# 微服务 vs 单体架构：AI 应用的架构选型分析

> 作者：孙架构 | 日期：2026-06-15 | 状态：决策已定

---

## 背景

公司在 Q2 末启动了 AI Research Copilot 项目。技术选型阶段，架构组内对「微服务还是单体」产生了激烈争论。本文档记录完整的分析过程和最终决策。

---

## 一、两个方案的详细定义

### 方案 A：微服务架构

```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Agent    │  │ Search   │  │ Write    │  │ Review   │
│ Service  │  │ Service  │  │ Service  │  │ Service  │
│ (Port    │  │ (Port    │  │ (Port    │  │ (Port    │
│  8001)   │  │  8002)   │  │  8003)   │  │  8004)   │
└────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │             │
     └─────────────┼─────────────┼─────────────┘
                   │             │
              ┌────┴────┐   ┌────┴────┐
              │ Redis   │   │ PostgreSQL│
              └─────────┘   └──────────┘
```

- 每个 Agent 角色（Search / Analyze / Write / Review）是独立的微服务
- 服务间通过 gRPC 通信
- 每个服务有独立的数据库
- K8s 部署，独立扩缩容

### 方案 B：单体架构（最终选择 ✅）

```
┌─────────────────────────────────────────┐
│              FastAPI 单体应用             │
│  ┌──────────────────────────────────┐   │
│  │        Unified Agent Loop        │   │
│  │  LLM → 选择工具 → 执行 → 观察    │   │
│  └──────────────────────────────────┘   │
│  ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 搜索工具  │ │ 分析工具  │ │ 写作工具 │ │
│  └──────────┘ └──────────┘ └─────────┘ │
│  ┌──────────────────────────────────┐   │
│  │   SQLite + ChromaDB（同进程）     │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

- 所有功能在一个 FastAPI 进程里
- 不同 Agent 角色以「工具」形式注册，不是独立服务
- SQLite + ChromaDB 本地运行

---

## 二、多维度对比

### 2.1 开发效率

| 维度 | 微服务 | 单体 |
|------|--------|------|
| 初始搭建时间 | 2-3 天（K8s + CI/CD + 服务脚手架） | 2 小时 |
| 本地调试 | 需要同时起 4+ 个服务 | `uvicorn main:app` 一行 |
| 新增 Agent 能力 | 新建服务 + 注册 gRPC + 配 K8s | `@registry.register()` 装饰器，10 行代码 |
| 代码理解成本 | 高（跨服务调试困难） | 低（状态在同一个 AgentState 里） |

### 2.2 性能

微服务在「独立扩缩容」方面有优势，但我们的场景里**瓶颈在 LLM API 调用（IO 等），不在计算**。

实测数据（同机，100 并发 SSE 连接）：
- 微服务架构（gRPC 通信）：P95 延迟 **18.7s**
- 单体架构（函数调用）：P95 延迟 **14.2s**

微服务反而慢 **24%**。原因是 gRPC 序列化/网络往返的额外开销。Agent 每次工具调用如果走 gRPC（而非函数调用），会增加 15-30ms 的额外延迟。一个深度研究任务平均 10 次工具调用，累积额外延迟 150-300ms。

### 2.3 团队规模适配

> 这个项目的开发团队是 **2-3 人**。微服务的运维成本（K8s、服务发现、链路追踪、日志聚合）需要至少 5 人的平台工程团队支撑。在 2-3 人团队里引入微服务，等于把有限的人力从「开发业务功能」转移到了「维护基础设施」——这是本末倒置。

### 2.4 未来迁移成本

当前选择单体，不代表永远单体。SQLAlchemy 2.0 ORM 支持一行配置从 SQLite 切换到 PostgreSQL。如果未来用户量增长需要拆分服务，也只需要拆成 3 个服务而非 10 个：

```
未来拆分方案（如果需要）：
  Service 1: Agent Engine（核心 Loops + 工具调度）
  Service 2: Knowledge Service（知识库管理 + 检索）
  Service 3: API Gateway（用户认证 + 限流 + 路由）
```

---

## 三、最终决策

### 决策：方案 B（单体架构）

**为什么选 A 不选 B？** 文档开头故意写反了。微服务才是方案 A。我们选了单体（方案 B）。

5 条理由，按重要性排序：

1. **团队规模不匹配**：2-3 人的团队维护微服务是资源浪费
2. **瓶颈在 LLM API 而非计算**：独立扩缩容的优势在 IO 密集型场景不存在
3. **Agent 的 Plan State 是共享状态**：微服务间拆分会破坏状态的连贯性——Agent 的 LLM 需要「看到所有历史工具调用结果」，拆成服务后要么传巨大的上下文（网络开销），要么各服务自己维护不完整的状态
4. **实测数据不支持微服务**：gRPC 通信的额外延迟让性能反而下降了 24%
5. **KISS 原则**：项目阶段是 0-1，不是 1-N。0-1 阶段需要快速迭代验证假设，微服务的复杂度会拖慢迭代速度

### 不选微服务的「反后悔药」

如果未来出现以下任一情况，立即启动微服务拆分：
- 单一团队超过 15 人
- 知识库检索成为独立产品（有独立的业务逻辑和团队）
- 日活超过 10 万，需要异地多活部署

---

## 四、经验总结

> **架构选型的第一原则：不是「什么架构最好」，是「什么架构最适合当前团队和当前阶段」。**
>
> 微服务不是银弹。Martin Fowler 在 2015 年提出的「Monolith First」原则到今天依然成立——先用单体验证产品，找到真正的业务边界后，再有针对性地拆分。不要为了「面试时可以讲微服务」而做微服务。

这个决策记录会放在团队 Wiki，供后续项目参考。

---

## 补充数据（2026-07-18 更新）

### 1. 性能基准测试完整报告

测试环境：同机（16C32G），100 并发 SSE 连接，持续 30 分钟压测。测试工具：Locust 2.31，测试日期：2026-06-12

| 指标 | 微服务方案（gRPC） | 单体方案（函数调用） | 差异 |
|------|-------------------|-------------------|------|
| P50 延迟 | 4.2s | 3.1s | -26.2% |
| P95 延迟 | 18.7s | 14.2s | -24.1% |
| P99 延迟 | 32.5s | 25.8s | -20.6% |
| 平均延迟 | 8.3s | 6.7s | -19.3% |
| 最大延迟 | 47.1s | 38.4s | -18.5% |
| 吞吐量（TPS） | 12.8 | 15.3 | +19.5% |
| CPU 使用率（平均） | 68% | 52% | -23.5% |
| 内存占用（平均） | 4.2 GB | 2.8 GB | -33.3% |
| gRPC 额外延迟/调用 | 15-30ms | 0ms | — |
| 深度研究任务累积额外延迟（10 次工具调用） | 150-300ms | 0ms | — |

结论：在 IO 密集型 AI Agent 场景下，微服务的通信开销和资源消耗均显著高于单体方案，且未带来预期的性能收益。

### 2. 基础设施成本对比表（月度）

| 费用项 | 微服务方案 | 单体方案 |
|--------|-----------|---------|
| 计算资源 | K8s 3 节点（8C16G × 3）¥8,640/月 | 2 台 EC2（4C8G × 2）¥3,840/月 |
| 缓存 | Redis 集群 ¥1,200/月 | 进程内缓存 ¥0 |
| 数据库 | PostgreSQL 托管 ¥2,400/月 | SQLite（本地）¥0 |
| 负载均衡 | ALB + Nginx Ingress ¥800/月 | Nginx 反向代理 ¥0 |
| 消息队列 | RabbitMQ ¥600/月 | 不适用 ¥0 |
| 日志收集 | ELK Stack ¥1,200/月 | 文件日志 ¥0 |
| 监控系统 | Prometheus + Grafana ¥400/月 | 不适用 ¥0 |
| **月度合计** | **¥15,240/月** | **¥3,840/月** |
| **年度合计** | **¥182,880/年** | **¥46,080/年** |

**年度差额：¥136,800**。对 2-3 人团队而言，这部分成本完全可以投入到更多 LLM API 调用或更高质量的模型上。

### 3. 团队规模-架构匹配决策矩阵

| 团队规模 | 推荐架构 | 推荐度 | 边界条件 |
|----------|---------|--------|---------|
| 1-3 人 | 单体 | ⭐⭐⭐⭐⭐ | 不允许使用微服务，除非有现成的平台工程团队支持 |
| 1-3 人 | 模块化单体 | ⭐⭐⭐ | 仅在代码结构已明显暗示未来拆分边界时采用 |
| 4-8 人 | 模块化单体 | ⭐⭐⭐⭐⭐ | 优先推荐，禁止跨模块直接访问数据库 |
| 4-8 人 | 微服务 | ⭐⭐ | 除非有专职 DevOps/SRE 人员，否则不应选择 |
| 9-15 人 | 模块化单体 | ⭐⭐⭐⭐ | 可配合事件总线做内部解耦 |
| 9-15 人 | 微服务 | ⭐⭐⭐ | 需配备至少 1 名专职 SRE 才允许采用 |
| 16-30 人 | 微服务 | ⭐⭐⭐⭐⭐ | 推荐按业务域拆分为 3-5 个服务 |
| 16-30 人 | 模块化单体 | ⭐⭐⭐ | 仅在现有单体已稳定运行且拆分成本过高时保留 |
| 30+ 人 | 微服务 | ⭐⭐⭐⭐⭐ | 必须实施微服务，否则代码协作成本将指数级上升 |
| 30+ 人 | 单体 | 不推荐 | 除非是特殊场景（如嵌入式系统、硬实时系统） |

**例外情况**：任何规模下，如果团队中有超过 50% 成员没有微服务运维经验，不应强行采用微服务架构。

### 4. 模块化单体（Modular Monolith）中间方案

作为方案 C 的完整描述：模块化单体是单体架构的一种有纪律的实现方式，它在代码层面严格划分模块边界，但部署时仍为一个进程。

**目录边界约定**：

```
src/
  modules/
    agent/          # Agent 核心循环
      interfaces/   # 公开接口（其他模块可调用）
      internal/     # 内部实现（其他模块不得引用）
    search/         # 搜索模块
    knowledge/      # 知识库管理
    auth/           # 用户认证
  shared/           # 共享工具（DTO、异常定义、常量）
```

**依赖规则**：
- 模块间不得直接 import 内部实现（`internal/` 目录）
- 模块间通信必须通过接口层（`interfaces/` 目录下的抽象类或 Protocol）
- 不允许循环依赖——使用 `import-lint` 工具在 CI 中自动检测
- `shared/` 不得依赖任何模块

**API 模块间通信方式**：
1. 同步场景：直接 Python 函数调用（通过接口层）
2. 异步场景：in-process 事件总线（自定义 `EventBus` 类，使用 `asyncio.Queue`）
3. 跨模块事务：通过 `UnitOfWork` 模式统一管理，不允许一个模块直接操作另一个模块的数据库表

**未来拆分为微服务的 4 个前置条件**：
1. 模块接口边界已稳定运行 ≥ 3 个月，无重大变更
2. 该模块的独立请求量 ≥ 单体总请求量的 30%
3. 团队规模 ≥ 15 人，且至少有 2 名成员熟悉该模块全部代码
4. 已建立完备的自动化 CI/CD 管线（测试覆盖率 ≥ 80%，部署时间 ≤ 10 分钟）

不符合以上任一条件时，禁止进行微服务拆分。

### 5. 不该使用微服务的 8 条明确禁忌

| # | 禁忌规则 |
|---|---------|
| 1 | **团队 < 15 人且无专职 SRE** 不应使用微服务——运维负担会挤占 40% 以上的开发时间 |
| 2 | **0→1 阶段产品**不应过早拆分——产品方向尚在验证，微服务的灵活性无法弥补其复杂度 |
| 3 | **数据库无独立扩展需求时**不应分库——强行分库会引入分布式事务、最终一致性等复杂问题 |
| 4 | **跨服务事务占比 > 20% 时**禁止强行微服务化——分布式事务的协调成本将远超收益 |
| 5 | **没有自动化 CI/CD 管线时**不允许部署微服务——手动部署微服务的故障率是自动化的 7-10 倍 |
| 6 | **网络延迟敏感型计算（< 5ms 要求）** 禁止跨服务调用——gRPC 的 15-30ms 额外开销在此场景不可接受 |
| 7 | **单表数据量 < 1000 万行时**不需要分库——SQLite 单表处理 1000 万行数据完全胜任，引入分库得不偿失 |
| 8 | **日志和监控体系未建立前**禁止上线微服务——没有全链路追踪和集中日志，微服务故障排查时间会延长 5-10 倍 |
